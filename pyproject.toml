[project]
name = "inventory-management-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pre-commit>=4.5.0",
]

[tool.ruff]
lint.ignore = ["F401", "E402", "F841"]
